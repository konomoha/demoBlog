{% extends "base_admin.html.twig" %}
{% block title %}
    Blog Symfony|Update
{% endblock %}

{% block body %}
<h1>Profil de {{name}}</h1>

    {{form_start(userForm)}}

        {{form_row(userForm.roles)}}

        <button>Modifier</button>
    {{form_end(userForm)}}


{% endblock %}