{% extends "base.html.twig" %}

{% block title %}
Blog Symfony | 

 {% if editMode %}
        Modification de l'article 
    {% else %}
        {# Sinon, l'article ne possède pas d'id, c'est une insertion #}
        Nouvel article 
    {% endif %}  

{% endblock %}

{% block body %}
    <h1 class="text-center my-5">
    {# Si editMode renvoie TRUE, cela veut dire que l'id possède un ID, c'est une modification #}
    {% if editMode %}
        Modification de l'article 
    {% else %}
        {# Sinon, l'article ne possède pas d'id, c'est une insertion #}
        Nouvel article 
    {% endif %}    
    </h1>

{# Première version du formulaire #}
{# <form method="post">

    <div class="mb-3">
        <label for="titre" class="form-label">Titre</label>
        <input type="text" class="form-control" id="titre" name="titre">
    </div>

    <div class="mb-3">
        <label for="contenu" class="form-label">contenu</label>
        <textarea type="text" class="form-control" id="contenu" rows="10" name="contenu"></textarea>
    </div>

    <div class="mb-3">
        <label for="photo" class="form-label">Photo</label>
        <input type="text" class="form-control" id="photo" rows="10" name="photo">
    </div>

<button type="submit" class="btn btn-dark mb-5">Enregistrer</button>

</form> #}

{# Deuxième version du formulaire #}

{{ form_start(formArticle)}}

    {{ form_row(formArticle.titre)}}

    {{ form_row(formArticle.contenu)}}
    
    {{ form_row(formArticle.photo)}}

    {# Si photoActuelle retourne TRUE, cela veut dire que l'article possède une photo, c'est une modification  #}

    {% if photoActuelle %}
        <p>
            <em>Vous pouvez uploader une nouvelle photo si vous souhaitez la modifier</em>
            <img src="{{ asset('uploads/photos/' ~ photoActuelle)}}"  class="col-12 img-responsive">
        </p>

    {% endif %}

    <button class="btn btn-dark">

         {% if editMode%}
        Modifier
    {% else %}
        {# Sinon, l'article ne possède pas d'id, c'est une insertion #}
        Enregistrer
    {% endif %}  

    </button>

{{ form_end(formArticle)}}

{% endblock %}